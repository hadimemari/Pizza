# Database (PostgreSQL - Supabase via Vercel)
# These are auto-set by Vercel when you connect Supabase
DATABASE_URL="postgresql://user:pass@host:5432/pizzamotion?pgbouncer=true"
DIRECT_URL="postgresql://user:pass@host:5432/pizzamotion"

# Session Secret - Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="change-me-in-production-use-openssl-rand"
NEXTAUTH_URL="http://localhost:9002"

# ─── sms.ir Configuration ───
# 1. Create account at https://sms.ir
# 2. Get your API key from: برنامه‌نویسان ← لیست کلیدهای API ← ایجاد کلید جدید
#
# --- Sandbox Mode (development) ---
# Create a Sandbox API key (نوع: Sandbox)
# Use template ID 123456 (built-in sandbox template: "کد تایید شما: #CODE#")
# No real SMS sent, no credit deducted
#
# --- Production Mode ---
# Create a Production API key
# Create a verification template with parameter "Code"
# Template must include a website with اینماد (e-Namad)
#
# Leave both empty for demo mode (OTP codes logged to server console)
SMSIR_API_KEY=""
SMSIR_TEMPLATE_ID="123456"
